# 2026-06-25 22:52:09 +08:00 跑量图切换与坐标轴布局调整

## 改动内容
- 将原本分开的周跑量图和月跑量图合并到同一个图表位置。
- 增加“周跑量 / 月跑量”切换按钮，选择后只展示对应跑量图。
- 保留体重关系散点图和配速-心率散点图。
- 统一多张图的纵轴标题位置，将轴标题放到坐标轴中部，减少与图例重合。
- 调整图表总高度，从 5 个图表区域收敛为 4 个图表区域，页面更紧凑。

## 为什么这样改
- 周跑量和月跑量属于同一类训练负荷信息，放在同一个区域切换更符合阅读逻辑。
- 第 3 张图的纵轴标题原本靠近顶部，容易和图例视觉重叠；统一轴标题位置可以提升可读性。
- 减少一个独立图表区域后，主页顶部图表不会被拉得过长。

## 解决的问题
- 解决图 4 和图 5 分开显示导致底部区域过长的问题。
- 解决配速-心率图中纵轴标签和图例重合的问题。
- 让各图的坐标轴排布更统一，后续继续扩展图表时更容易维护。

## 验证
- `npm run test` 通过：3 个测试文件，12 个测试全部通过。
- `npm run build` 通过。
- 构建仍有 ECharts 大包警告，这是已有的体积提示，不影响本地运行。
