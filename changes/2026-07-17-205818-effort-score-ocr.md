# Apple Watch 耗能评分 OCR 校准记录

- 修改时间：2026-07-17 20:58:18 +08:00（Asia/Shanghai）
- 修改范围：本地源码与自动化测试
- 部署状态：未部署
- 数据写入状态：仅验证表单预填，未保存跑步记录
- 修改前备份：`backups/2026-07-17-204948-effort-score-crop-ocr/`

## 问题现象

使用 `7c11fb6773ead8b89579ab52dd2a176f.jpg` 做真实上传验证时，日期时间、距离、总用时、平均配速、平均心率、平均步频和平均功率均能正确回填，但整图 OCR 无法可靠读出紫色圆圈中的耗能评分 `9`，页面提示“未可靠识别：耗能评分”。

初版宽区域裁剪曾把评分误读为 `5`，因此未保留该方案；继续缩小识别区域并增加图像预处理后才通过原图验证。

## 修改内容

1. 在总览识别模式中增加耗能评分的定向二次 OCR；单段识别流程不执行该步骤。
2. 只有整图文本包含“耗能”或“体能训练详细信息”时才启用评分识别，避免把分段截图中的其他数字当作评分。
3. 按 Apple Watch 总览页比例裁剪左下方评分圆圈，并放大 4 倍。
4. 对裁剪图做亮色字形与深色背景的二值化处理，提高紫色数字在深色卡片上的对比度。
5. 定向识别使用单字符模式并限制为数字 `0-9`，识别结果以“耗能评分”标签加入解析文本。
6. 浏览器内置文字识别路径也增加对应的评分区域补充识别。
7. 增加“竭尽全力”对应评分 `9` 的文本校正规则与回归测试。
8. 补充总爬升高度解析，并兼容本张图中 `2 米` 被识别成 `2K` 的两栏 OCR 结果。

## 解决的问题

- 修复了本张截图中耗能评分缺失的问题。
- 避免宽裁剪区域造成的错误数字回填。
- 保持原有总览字段、开始时间识别和分段识别逻辑不变。

## 原图端到端验证结果

| 字段 | 自动填入值 | 结果 |
| --- | ---: | --- |
| 日期时间 | `2026-07-16T22:07` | 正确，使用跑步开始时间 |
| 距离 | `5.01` km | 正确 |
| 总用时 | `0:30:53` | 正确 |
| 平均配速 | `6:09` /km | 正确 |
| 平均心率 | `174` bpm | 正确 |
| 平均步频 | `166` spm | 正确 |
| 平均功率 | `248` W | 正确 |
| Apple Watch 耗能评分 | `9` | 正确 |
| 累计爬升 | `2` m | 正确 |

页面最终提示：“已识别总览数据，请校对后再保存。”浏览器控制台无警告或错误。

## 验证记录

- OCR 专项测试：`10/10` 通过，其中包含本张图片实际组合 OCR 文本的完整字段断言。
- 完整测试集：`8` 个测试文件、`53/53` 个测试通过。
- 生产构建：通过，Vite 共转换 `641` 个模块。
- 构建仍只有既有的大体积 chunk 提示，不影响本次功能。

## 回退方式

如需回退本次 OCR 校准，可从 `backups/2026-07-17-204948-effort-score-crop-ocr/` 恢复 `App.tsx.before`、`ocr.ts.before` 和 `ocr.test.ts.before`。

## 后续注意

评分圆圈裁剪使用 Apple Watch 当前总览页面的相对位置。若以后 Apple 调整页面布局，或截图经过大幅裁边，需要用新样本再次校准裁剪比例。
