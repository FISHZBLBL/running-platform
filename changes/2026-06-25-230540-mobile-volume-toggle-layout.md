# 2026-06-25 23:05:40 +08:00 手机端跑量切换按钮布局调整

## 改动内容
- 缩小手机端“周跑量 / 月跑量”切换按钮的字号、内边距和最小高度。
- 将手机端切换按钮移动到跑量图右上侧，减少对左侧图例的占用。
- 将跑量图图例略微下移，并相应调整图表区域顶部位置，避免图例、按钮和坐标轴互相重叠。

## 为什么这样改
- 手机宽度较窄，原按钮尺寸会挤占图例空间，导致“最长单次”等图例文字和按钮重叠。
- 让按钮靠右并缩小后，图例可以继续从左侧自然排列，阅读更清楚。

## 解决的问题
- 解决手机端图 4 中“周跑量 / 月跑量”按钮与图例文字重叠的问题。
- 保持桌面端布局不变，只针对窄屏做适配。

## 验证
- `npm run test` 通过：3 个测试文件，12 个测试全部通过。
- `npm run build` 通过。
- 已用 627px 宽度预览检查，切换按钮宽约 106px、高约 32px。
- 构建仍有 ECharts 大包警告，这是已有的体积提示，不影响本地运行。
